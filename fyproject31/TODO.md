# TODO

- [ ] Update homepage login button to point to Laravel route `/login` and change text to `Login`.
- [ ] Add `GET /login` route to return `view('login')`.
- [ ] (Optional) Re-check for any other links still pointing to `login.php`.

