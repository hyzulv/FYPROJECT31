<IfModule mod_rewrite.c>
    RewriteEngine On

    # Force HTTPS (recommended for InfinityFree)
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]

    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>